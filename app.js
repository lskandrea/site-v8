const cfg=window.MJSC_CONFIG||{};const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];
function attr(id,a,v){const e=document.getElementById(id);if(e&&v)e.setAttribute(a,v)}function txt(id,v){const e=document.getElementById(id);if(e&&v)e.textContent=v}
$$('[data-instagram]').forEach(e=>e.href=cfg.instagramUrl);$$('[data-facebook]').forEach(e=>e.href=cfg.facebookUrl);$$('[data-drive]').forEach(e=>e.href=cfg.googleDriveFolderUrl);$$('[data-form]').forEach(e=>e.href=(cfg.googleFormEmbedUrl||'').replace('embedded=true','usp=sharing'));txt('clubAddress',cfg.address);txt('clubPhone',cfg.phone);txt('clubEmail',cfg.email);attr('clubEmail','href','mailto:'+cfg.email);attr('clubPhone','href','tel:'+(cfg.phone||'').replaceAll(' ',''));attr('formFrame','src',cfg.googleFormEmbedUrl);attr('mapFrame','src',`https://www.google.com/maps?q=${encodeURIComponent(cfg.address||'Lycée des Iscles Manosque')}&output=embed`);
const burger=$('.burger'),links=$('.links');burger?.addEventListener('click',()=>links.classList.toggle('open'));$$('.links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));
const slides=$$('.heroSlide'),dots=$$('.dots span');let i=0;function show(n){if(!slides.length)return;i=(n+slides.length)%slides.length;slides.forEach((s,k)=>s.classList.toggle('active',k===i));dots.forEach((d,k)=>d.classList.toggle('on',k===i))}$('.next')?.addEventListener('click',()=>show(i+1));$('.prev')?.addEventListener('click',()=>show(i-1));if(slides.length)setInterval(()=>show(i+1),5200);
const back=$('.backTop');window.addEventListener('scroll',()=>back?.classList.toggle('show',scrollY>450));back?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
